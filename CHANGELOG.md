# Changelog

## Unreleased

- Menubar UI improvements: custom feeds, per-language voices, speech rate, summary length.
- Better status reporting (duration, item count, feed error count).
- Add "Stop speaking" during a run.
- Add tests (config validation, feed parsing) and CI.

## 1.0.0

- Initial release: RSS fetch, summarization, macOS `say`, LaunchAgent scheduling.
